# 🚀 Как проверить тесты

## 📍 Где находятся тесты:

```
backend/tests/
├── unit/              ← UNIT ТЕСТЫ
└── integration/       ← INTEGRATION ТЕСТЫ
```

## ✅ Способ 1: Запустить ВСЕ тесты (рекомендуется)

### В терминале PowerShell:

```powershell
cd backend
npm test
```

### Что вы увидите:
- Список всех тестов с результатами (✔ или ✖)
- Общее время выполнения
- Количество пройденных/проваленных тестов

---

## ✅ Способ 2: Запустить только UNIT тесты

```powershell
cd backend
node --test tests/unit/**/*.test.js
```

---

## ✅ Способ 3: Запустить только INTEGRATION тесты

```powershell
cd backend
node --test tests/integration/**/*.test.js
```

---

## ✅ Способ 4: Запустить конкретный файл

```powershell
cd backend

# Пример: unit тесты middleware
node --test tests/unit/middleware.test.js

# Пример: integration тесты auth
node --test tests/integration/auth.test.js
```

---

## 📋 Пошаговая инструкция:

1. **Откройте терминал** (PowerShell или Command Prompt)

2. **Перейдите в папку backend:**
   ```powershell
   cd "C:\Users\Alser\OneDrive\Документы\2 course AlmaU\Backend\Dragon\backend"
   ```
   Или просто:
   ```powershell
   cd backend
   ```
   (если вы уже в корне проекта)

3. **Запустите тесты:**
   ```powershell
   npm test
   ```

4. **Дождитесь результатов** - вы увидите список всех тестов

---

## ⚠️ Важно перед запуском:

Убедитесь, что **dev-сервер НЕ запущен** на порту 8080!

Если он запущен:
- Остановите его (Ctrl+C в терминале, где он работает)
- Или закройте этот терминал

---

## 📊 Что показывают результаты:

- **✔** - тест пройден успешно
- **✖** - тест провален (есть ошибка)
- Время выполнения каждого теста
- Общая статистика в конце

---

## 🎯 Быстрая проверка:

```powershell
# 1. Перейти в backend
cd backend

# 2. Запустить тесты
npm test
```

**Готово!** Вы увидите результаты всех 84+ тестов.

